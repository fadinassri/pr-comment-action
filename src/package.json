{
  "name": "pr-comment-action",
  "version": "1.0.0",
  "description": "A GitHub Action to comment on PRs",
  "main": "index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
